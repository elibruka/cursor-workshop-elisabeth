name: Exception Triage
on:
  workflow_dispatch:
  schedule:
    - cron: "0 8 * * *"

permissions:
  contents: read
  issues: write
  id-token: write

jobs:
  top-errors:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: >-
            {"clientId":"${{ secrets.AZURE_CLIENT_ID }}","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"${{ secrets.AZURE_SUBSCRIPTION_ID }}","tenantId":"${{ secrets.AZURE_TENANT_ID }}"}

      - name: Ensure Log Analytics extension
        run: az extension add --name log-analytics --upgrade

      - name: Create tasks for top 3 findings with Claude
        uses: anthropics/claude-code-action@v1
        env:
          ANTHROPIC_BASE_URL: ${{ vars.ANTHROPIC_BASE_URL }}
          LOG_ANALYTICS_WORKSPACE_ID: ${{ vars.LOG_ANALYTICS_WORKSPACE_ID }}
        with:
          anthropic_api_key: ${{ secrets.ANTHROPIC_FOUNDRY_API_KEY }}
          claude_args: |
            --model claude-sonnet-4-6
            --dangerously-skip-permissions
          prompt: |
            Read exceptions from Log Analytics and create up to 3 separate GitHub issues in this repository, one issue per top finding.

            Use this Log Analytics command as the reference query:
            ```bash
            az monitor log-analytics query \
              --workspace "a53c2ee0-9900-4756-b9c1-ab1b96a28d87" \
              --analytics-query "exceptions | where timestamp > ago(24h) | summarize Count=count(), LatestOccurrence=max(timestamp), EarliestOccurrence=min(timestamp) by problemId, type, outerMessage | top 2 by Count desc" \
              --output json
            ```

            Requirements:
            - Create at most 3 issues.
            - Use title format:
              [Auto][Log Analytics] Critical finding #<n> - <Signature>
            - Use body format:
              Automated task created from Azure Log Analytics (last 24h).

              - Severity: Critical
              - Occurrences: <Occurrences>
              - Last seen (UTC): <LastSeen>
              - Workspace ID: ${LOG_ANALYTICS_WORKSPACE_ID}
              - Problem ID: <ProblemId>
              - Exception type: <ExceptionType>
              - Source tables: <SourceTables>

              Sample message:
              <SampleMessage>

            Use GitHub CLI (`gh issue create`) and print the created issue URLs at the end.